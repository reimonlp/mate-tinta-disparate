services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    container_name: mate-tinta-disparate
    networks:
      - proxy-network
    volumes:
      - .:/app

networks:
  proxy-network:
    external: true
